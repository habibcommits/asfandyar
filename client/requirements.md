## Packages
framer-motion | Page transitions and animations
lucide-react | Icons for the UI
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
API calls use credentials: "include" for auth cookies.
Images are served from external URLs or Unsplash placeholders for now.
The admin panel is protected and requires specific credentials (admin/admin admin).
